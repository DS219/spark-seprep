# Soeun Youn

My favorite language is *Python* because it is easy to read and useful for data science.

## Example code

```python
numbers = [1, 2, 3, 4]
print(sum(numbers))
```

This code creates a list of four numbers and prints the sum of the numbers.
