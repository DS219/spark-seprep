# Berk Gozek

Hi, my name is Berk and my favorite programing language is PhP because its the most viable programming language for Content Management Systems(It's actually c# and I like making games but dont tell the employers that).

## Example code

```php
<?php
function greet(string $name): string {
    return "Hello, {$name}! Welcome to my CMS.";
}

echo greet("World") . PHP_EOL;
```

### Code Explanation

This script defines a `greet` function that takes a name and returns a friendly greeting string, then prints it to the console. To run it, save the code to a file named `greet.php` and run `php greet.php` from the terminal. You'll need PHP installed. See the [PHP install documentation](https://www.php.net/manual/en/install.php).
