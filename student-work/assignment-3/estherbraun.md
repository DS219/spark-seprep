# Esther Braun

Hi, my name is Esther and my favorite programming language is Python. When I took DS 110 I almost failed the class, but I kept working at it and slowly got better. Now Python is a reminder that if I put in the effort, I can achieve things I didn't expect to — which is why it's my favorite.

## Example code

```
import random

jokes = [
    "Why do programmers prefer dark mode? Because light attracts bugs!",
    "Why did the Python programmer go broke? Because he ran out of cache!",
    "I would tell you a joke about UDP, but you might not get it.",
    "Why was the developer unhappy at their job? They wanted arrays!",
]

print(random.choice(jokes))
```

### Code Explanation

This script prints a random corny dad joke each time you run it. It stores a list of jokes, uses `random.choice()` to pick one at random, and prints it to the screen. To run it, save the code to a file called `jokes.py`, then run `python jokes.py` in your terminal. You'll need Python 3 installed — see the [Python install docs](https://www.python.org/downloads/).
