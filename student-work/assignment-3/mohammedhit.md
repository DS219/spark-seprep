# Mohammed Hit
I am a senior studying CS and my favorite programming language is Python because it's very simple and easy to quickly mock up software. 

## Example code

```
def introduction(name):
	print("Hello ", name)
introduction("Mohammed")
```

### Code Explanation
The above code defines an introduction method in Python with a parameter `name`. 
Then I call the method with an arugment of my name, `Mohammed` causing it to say
`Hello Mohammed`. To run the code, we need to create a Python file with the above code,
e.g., `introduction.py` and run `python introduction.py` in the command lin.  

