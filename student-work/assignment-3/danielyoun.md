# Daniel Youn

My favorite programming language is Python because it is easy to read and write. I like Python because its simple syntax makes it easier to focus on solving problems and creating programs.

## Example code

```python
name = "Daniel Youn"
print("Hello, " + name + "!")
```

### Code Explanation

This code stores my name in a variable called `name` and then prints a greeting to the screen. To run the code, save it as a Python file and run it using Python in the terminal.