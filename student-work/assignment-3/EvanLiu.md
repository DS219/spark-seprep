# Evan Liu

Hi, my name is Evan and my favorite coding language is Python because of how versatile it is. 

## Example code

```python
def greet(name):
    return f"Hello, {name}!"

print(greet("World"))
```

### Code Explanation

This function takes a name as input and returns a greeting string. To run it, save the code in a `.py` file and execute it with `python filename.py` — it will print `Hello, World!` to the console.