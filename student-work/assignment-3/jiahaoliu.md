# Jiahao Liu

My favorite programming language is Python because it is easy to use and very useful for data science.

## Example code

```python
numbers = [1, 2, 3, 4, 5]
average = sum(numbers) / len(numbers)
print(average)
```

### Code Explanation

This code calculates the average of a list of numbers. It can be run with Python.
